# Claude focused re-review: acquisition correction at `fa3818b`

## Review target and verdict

- Reviewed commit: `fa3818b1426c004f7ed6cb0ed5f4cb37e474628d`
- Parent: `803ba5c25b3d5d86b41e5e9258f7fa6711e4cd1c`
- Scope: the offline-only correction to the bounded Nansen acquisition workflow
- Verdict: **not approved for the live pilot**

Claude's focused re-review reported that all five blockers in its earlier blocking set were corrected, then identified one remaining blocker at the provider-response boundary. This review's “five” is retained as supplied; the earlier project record grouped the original feedback into ten correction areas.

## Final blocking finding

Coverage requests sent a candidate wallet through the provisional `filters.trader_address` field, but the workflow did not validate returned wallets or tokens until terminal pagination. A provider that silently ignored the filter could therefore return another wallet on a valid nonterminal page. The workflow would settle that call as successful, cache it, and continue. This could spend the pilot and count calls without establishing candidate-specific coverage.

The required correction is per-page and fail-closed: normalize every coverage row immediately, compare canonical wallet and token addresses, settle any mismatch as `invalid-response` with `wallet-filter-not-applied`, preserve credit accounting, omit the page from success/cache/compilation, record only allowlisted aggregate evidence, and stop after that attempt. Empty pages remain provisional pagination evidence rather than proof that the filter was enforced.

## Review evidence available to this record

The supplied focused-review material identifies the silent-filter scenario and specifies the expected four-attempt synthetic schedule: three discovery calls followed by the first rejected coverage attempt. It also requires wrong-token, mixed-case, nonterminal, sanitization, and mutation regressions.

The material provided to Codex does not include Claude's model/effort setting, verbatim command output, a complete list of executed commands, or names/results for every probe. This record does not infer them. It records the concrete scenario and acceptance probes explicitly supplied with the re-review.

## Material non-blocking items

Breadth-first scheduling across candidate coverage work remains a post-pilot consideration. The current scheduler is intentionally unchanged by this focused correction. The supplied material refers to surviving non-blocking mutations but does not enumerate their exact source changes or probe output; they remain future review evidence and are not presented here as verified findings. No unrelated improvement is implemented in this disposition.

## Provider behavior still unverified

No tracked project source independently confirms `filters.trader_address`. Provider acceptance and enforcement, selectivity, pagination stability, terminal behavior, date boundaries, direction semantics, stable leg identity, multi-leg USD semantics, complete-window cost, and redistribution remain unverified. The focused re-review did not authorize a live request or approve the pilot.

## Disposition

Adapter/state/schema version 3 validates every coverage page before success accounting or caching. The synthetic ignored-filter regression stops after exactly four attempts, records three acquisition successes, retains all four reported credits, compiles no candidate, and emits only aggregate coverage-page evidence. Wrong-token and case-normalization tests exercise the same validator. The automatic network guard and an explicit temporary mutation verify that bypassing the per-page rejection makes the focused regression fail.

This disposition made zero Nansen or other external application calls and consumed zero credits. A new independent review remains required before any separate pilot authorization.
